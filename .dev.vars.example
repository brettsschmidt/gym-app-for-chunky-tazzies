# Copy this file to .dev.vars (gitignored) and fill in real values.
# These are read by `wrangler dev` and `opennextjs-cloudflare preview` for local
# development. In production, set the same keys as Worker secrets in the
# Cloudflare dashboard.

SUPABASE_SERVICE_ROLE_KEY=
TOTP_ENCRYPTION_KEY=
SHARE_LINK_SECRET=
CRON_SECRET=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=
ADMIN_EMAILS=
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
